apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: godfs-secretstore
  namespace: godfs
spec:
  # This is an EXAMPLE. Replace with your actual backend:
  # - Vault
  # - AWS Secrets Manager
  # - GCP Secret Manager
  # - Azure Key Vault
  provider:
    fake:
      data:
        - key: "godfs/cluster_key"
          value: "CHANGE_ME"

